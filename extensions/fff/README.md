# @local/fff

Lean fork of [`@ff-labs/pi-fff`](https://github.com/dmtrKovalenko/fff/tree/main/packages/pi-fff): same engine, stock-shaped `find`/`grep`.

- override mode (no `fffind`/`ffgrep`)
- no prompt guidelines
- tight defaults + stock truncation
- no multi_grep

```json
{ "packages": ["extensions/fff"] }
```

`/fff-mode` · `/fff-health` · `/fff-rescan`
